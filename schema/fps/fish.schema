package fps;

import "fps/common.schema";

enum EFishType
{
    NORMAL = 0;
    SPEED = 1;
    OCTOPUS = 2;
}

enum EFishState
{
    SWIM = 0;
    DEAD = 1;
    IDLE = 2; 
}

component FishComponent {
    id = 2070;
    
    EFishType type = 1;
    EFishState state = 2;
    fps.Vector3Int destination = 3;
}
    